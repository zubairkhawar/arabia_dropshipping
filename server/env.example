# Copy this file to .env and fill in your values.
# Required for the app to start:

# PostgreSQL connection string
# Local: postgresql://USER:PASSWORD@localhost:5432/DATABASE_NAME
# Cloud (e.g. Supabase/Neon/RDS): use the URL they provide
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/arabia

# Redis (optional; used for caching/sessions if you add that later)
REDIS_URL=redis://localhost:6379/0

# JWT secret for auth tokens (use a long random string)
JWT_SECRET_KEY=your-super-secret-key-change-in-production

# Optional: create first admin user on startup
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=your-admin-password

# Optional: API keys (can also set via Admin Settings in the app)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_TEMPERATURE=0.2

# Optional: Meta WhatsApp Cloud API
# META_WHATSAPP_ACCESS_TOKEN=EAA...
# META_WHATSAPP_VERIFY_TOKEN=your-verify-token
# META_WHATSAPP_PHONE_NUMBER_ID=123456789012345
# META_WHATSAPP_WABA_ID=1234567890123456
# META_GRAPH_API_VERSION=v21.0

# Customer bot handoff uses Agent.team (set per agent in routing/admin). Expected values:
# new_customer | beginner | intermediate | expert

# Optional: merchant store API (for AI customer/order context; omit if unused)
# Base URL should include API version prefix if required, e.g. https://www.backend.arabiadropship.com/v1
# CLIENT_API_BASE_URL=https://www.backend.arabiadropship.com/v1
# Bearer token (Authorization: Bearer …) — preferred for Arabia backend
# CLIENT_API_BEARER_TOKEN=
# Or API key header (X-API-Key) for other integrations
# CLIENT_API_KEY=

# Optional: environment
# ENVIRONMENT=development
# DEBUG=true
# FRONTEND_BASE_URL=http://localhost:3000
